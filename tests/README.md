# Tests

This directory contains tests to ensure the mouse rig implementation stays in sync with its contracts.

## Running Tests

```bash
python tests/test_contracts.py
```

## Test Files

- `test_contracts.py` - Validates that contract schemas match actual implementation, preventing drift between validation rules and code
